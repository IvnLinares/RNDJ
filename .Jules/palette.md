## 2026-01-14 - Added Password Visibility Toggle
**Learning:** Adding a password visibility toggle significantly improves usability, especially on mobile. Using `type="button"` on the toggle is critical to prevent accidental form submission inside a form element.
**Action:** In future forms with password fields, always include a visibility toggle by default using the `position-relative` wrapper pattern.
